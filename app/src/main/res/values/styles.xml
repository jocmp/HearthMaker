<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:android="http://schemas.android.com/apk/res/android">
    <style name="BaseTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowActionBarOverlay">false</item>
    </style>

    <style name="AppTheme" parent="@style/Theme.AppCompat.Light.NoActionBar"/>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="HearthMakerTheme" parent="@style/Theme.AppCompat.Light.NoActionBar" >
    </style>

    <style name="filter_style">
        <item name="android:background">@color/primary_material_light</item>
        <item name="android:popupBackground">@android:color/white</item>

        <item name="android:layout_marginStart">@dimen/activity_vertical_margin</item>
        <item name="android:layout_marginEnd">@dimen/activity_vertical_margin</item>
    </style>

    <style name="Theme.MyTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:actionBarWidgetTheme">@style/Theme.MyTheme.Widget</item>
        <!-- your other custom styles -->
    </style>
    <style name="Theme.MyTheme.Widget" parent="Widget.AppCompat.SearchView">
        <item name="android:textColorHint">@android:color/white</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:color">@android:color/white</item>
        <!-- your other custom widget styles -->
    </style>

</resources>